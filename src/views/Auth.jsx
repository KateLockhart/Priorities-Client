export const AuthView = () => {
    // TODO: Conditionally render login vs Sign Up forms
    return (
        <div>
            <h1>This is the Auth Page view.</h1>
            <h2>Login || Sign up</h2>
        </div>
    )
}